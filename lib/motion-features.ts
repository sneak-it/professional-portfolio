// Lazy feature bundle for LazyMotion. domAnimation covers enter/exit, variants,
// and hover/tap/inView gestures; domMax (layout/drag) is excluded to stay small.
export { domAnimation as default } from 'motion/react';
